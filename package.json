{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon index.js",
    "start": "node index.js",
    "build:ui": "node scripts/rm \"build\" && cd \"../fullstackopen\" && npm run build && cd \"../FSOBackend\" && node scripts/mv \"../fullstackopen/build\" \"./\"",
    "deploy": "git add . && git commit -m uibuild && git push origin master && git push heroku master",
    "logs:prod": "heroku logs --tail",
    "deploy:full": "npm run build:ui && npm run deploy"
  },
  "author": "Micheal",
  "license": "MIT",
  "dependencies": {
    "cors": "2.8.5",
    "express": "4.17.1"
  },
  "devDependencies": {
    "nodemon": "1.19.2",
    "shelljs": "0.8.3"
  }
}
